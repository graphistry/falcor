var prefix = require('./support/prefix');
var Keys = {
    any: prefix + 'keys',
    keys: prefix + 'keys',
    ints: prefix + 'integers',
    ranges: prefix + 'ranges',
    integers: prefix + 'integers',
    named: prefix + 'named',
    name: prefix + 'name',
    match: prefix + 'match',
};

module.exports = Keys;
