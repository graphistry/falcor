{
  "name": "falcorjs",
  "version": "1.0.0",
  "private": true,
  "description": "Graphistry's FalcorJS mono repo",
  "main": "index.js",
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap --concurrency=1",
    "test": "cross-env NODE_ENV=production MOCHA_COLORS=true lerna run --concurrency=1 test",
    "build": "cross-env NODE_ENV=production MOCHA_COLORS=true lerna run --concurrency=1 build",
    "prepublish": "cross-env NODE_ENV=production MOCHA_COLORS=true lerna run --concurrency=1 prepublish",
    "deploy": "cross-env NODE_ENV=production MOCHA_COLORS=true lerna publish --concurrency=1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/graphistry/falcor.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/graphistry/falcor/issues"
  },
  "homepage": "https://github.com/graphistry/falcor/blob/master/README.md",
  "devDependencies": {
    "cross-env": "~3.1.3",
    "lerna": "2.0.0-beta.30"
  }
}
